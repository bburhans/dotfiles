set guifont=DejaVu_Sans_Mono:h11,Consolas:h11
au GUIEnter * simalt ~x " Simulates Alt-Space-x to maximize the window on startup
colo murphy
set go+=P " Use the system clipboard instead of the default

